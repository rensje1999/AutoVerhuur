@model IEnumerable<WebApplication1.Models.Caterie>

@{
    
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.caterieNaam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.aantalPersonen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.aantalKoffers)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.caterieNaam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.aantalPersonen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.aantalKoffers)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.caterieId }) |
                @Html.ActionLink("Details", "Details", new { id=item.caterieId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.caterieId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
